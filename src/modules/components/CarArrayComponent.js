// import React from "react";
// import ShowCard from "./ShowCard";

export default function CarArrayComponent (search) {
    const searchParam = [...search.split(', ')];
    return console.log(searchParam)

} 
